# Backend API URL (optional in local dev as Vite proxies /api to http://localhost:8000)
# In production (e.g. Vercel), set this to your deployed FastAPI backend URL:
# VITE_API_URL=https://your-backend-api.onrender.com
VITE_API_URL=
